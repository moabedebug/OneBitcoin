import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    headePrice:{
        width: "100%",
        height: "auto",
        alignItems: "center",
    },
    CurrentPrice: {
        color: "#f50d41",
        fontSize: 32,
        fontWeight: "bold",
        paddingTop: 20
    },
    textPrice: {    
        color: "#ffffff",
        fontSize: 16,
        fontWeight: "bold",
    }
});

export default styles
